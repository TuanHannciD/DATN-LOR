@model IEnumerable<AuthDemo.Models.HangSX>
@{
    ViewData["Title"] = "Danh sách hãng sản xuất";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="card mt-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h4 class="mb-0"><i class="mdi mdi-cube-outline"></i> Danh sách hãng sản xuất</h4>
        <a class="btn btn-primary" href="@Url.Action("Create", "HangSX", new { area = "Admin" })">
            <i class="mdi mdi-plus"></i> Thêm hãng
        </a>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Tên hãng</th>
                        <th class="text-center">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
    @foreach (var item in Model)
    {
        <tr>
                        <td>@item.HangSXName</td>
                        <td class="text-center">
                            <a class="btn btn-warning btn-sm" href="@Url.Action("Edit", "HangSX", new { id = item.ID_Hang, area = "Admin" })">
                                <i class="mdi mdi-pencil"></i> Sửa
                            </a>
                                <a class="btn btn-danger btn-sm" href="@Url.Action("Delete", "HangSX", new { id = item.ID_Hang, area = "Admin" })" onclick="return confirm('Bạn có chắc muốn xóa?');">
                                <i class="mdi mdi-delete"></i> Xóa
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
</div>